{
    "name": "tvtcommerce/module-admin-audit-log-tests",
    "description": "Isolated PHPUnit environment for the Magento-independent parts of TVTCommerce_AdminAuditLog. Kept separate from the module's own composer.json because magento/module-* packages require repo.magento.com credentials and are not needed to unit-test plain-PHP classes.",
    "type": "project",
    "require": {
        "psr/log": "^3"
    },
    "require-dev": {
        "phpunit/phpunit": "^10"
    },
    "config": {
        "sort-packages": true
    }
}
